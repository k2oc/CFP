const prettier = require("prettier")

const fs = require('fs')
const path = require('path')
// prettier.format("<template>2222</template>" , {parser: 'vue'} )
// const result = prettier.format("<html></html>", { semi: false, parser: "html" });

// console.log( result )
